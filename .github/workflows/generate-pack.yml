name: pytest_code_converter
description: Converts Python test code to pytest format
inputs:
  python_code:
    description: Original Python test code to convert
    required: true
    type: string
outputs:
  pytest_code:
    description: Converted pytest-compatible code
    type: string
steps:
  - name: Parse input code
    uses: python
    with:
      script: |
        import ast
        code = inputs['python_code']
        tree = ast.parse(code)
        # Additional parsing logic here
  - name: Convert to pytest format
    uses: python
    with:
      script: |
        # Conversion logic here
        def convert_to_pytest(tree):
            # Implementation details
            return pytest_code
        pytest_code = convert_to_pytest(tree)
        outputs['pytest_code'] = pytest_code
