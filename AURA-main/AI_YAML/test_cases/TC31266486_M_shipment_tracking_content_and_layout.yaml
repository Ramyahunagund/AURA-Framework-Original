%YAML 1.2  
---
spec_version: "1.1"

test_case: "shipment_tracking_content_and_layout"
test_case_id: "C31266486, C31409313"
version: "1.0"       
description: "Test Instant Services Shipment Tracking content and layout"

flow:
  phases:
    setup:
      - file: instantservices_login.yaml
      
    steps:
      - file: instantservices_dashboard_shipment.yaml

      - id: TC_shipment_tracking_page_1
        page: instantink/Dashboard
        action: verify
        reference_image: ""
        screenshot: True      
        prompt: |
            f"""
              Use test image for validating structural and contents on the page.

              Ignore the navigation pane of the Test image.

              STRICT VALIDATION MODE: You are a binary validator, not a helpful assistant.,
              Return {{"status": false}} immediately if ANY condition fails.,
              Do not make excuses or justifications.,
              
              1. Your focus area is after the navigation pane.
              2. There must be exactly the word "Shipment Tracking" positioned at the top left. If not, immediately return {{"status": false}}.
              3. "instant ink" logo appears to the right of "Shipment Tracking". If not, immediately return {{"status": false}}.

              Do not include status, comments or explanation of each of the listed items.
              If all conditions are met return {{"status": true}}            
              """
        sleep: 5

      - id: TC_shipment_tracking_page_2
        page: instantink/Dashboard
        action: verify
        reference_image: ""
        screenshot: True      
        prompt: |
            f"""
              Use test image for validating structural and contents on the page.

              Ignore the navigation pane of the Test image.

              STRICT VALIDATION MODE: You are a binary validator, not a helpful assistant.,
              Return {{"status": false}} immediately if ANY condition fails.,
              Do not make excuses or justifications.,
              
              1. Your focus area is after the navigation pane.
              2. A "Select printer" drop down selector is positioned next to "instant ink". If not, immediately return {{"status": false}}.
              3. The "Select printer" drop down selector view must contain a printer icon and printer model. If not, immediately return {{"status": false}}.
              4. "Enroll or replace a printer", hyperlink should appear below drop down selector. If not, immediately return {{"status": false}}.
              5. Black and Color cartridges availability details mentioned here. If not, immediately return {{"status": false}}.
              6. You should find "Frequently Asked Questions" and "Cartridge Support" sections mentions in the "Current Plan" section.  If not, immediately return {{"status": false}}.

              Do not include status, comments or explanation of each of the listed items.
              If all conditions are met return {{"status": true}}            
              """
        sleep: 5

    cleanup:
      - file: instantservices_logout.yaml  